{
  "description": "Manage the state of services (without a puppet agent)",
  "input_method": "environment",
  "parameters": {
    "action": {
      "description": "The action (start, stop, restart) to perform on the service.",
      "type": "Enum[start, stop, restart]"
    },
    "name": {
      "description": "The name of the service to act on.",
      "type": "String[1]"
    }
  }
}
